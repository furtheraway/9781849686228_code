<Application x:Class="CH01.CustomAttached.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CH01.CustomAttached"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--<Style TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value=".5,.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="{Binding Path=(local:RotationManager.Angle), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}" />
                </Setter.Value>
            </Setter>
        </Style>-->
        <!--<Style TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value=".5,.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="{Binding Path=(ToolTipService.HorizontalOffset), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}" />
                </Setter.Value>
            </Setter>
        </Style>-->

    </Application.Resources>
</Application>
